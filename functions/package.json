{
  "name": "functions",
  "description": "Cloud Functions for Firebase",
  "scripts": {
    "serve": "firebase emulators:start --only functions",
    "shell": "firebase functions:shell",
    "start": "npm run shell",
    "deploy": "echo BLOCKED - unscoped deploy overwrites mobile-owned functions. Use: firebase deploy --only functions:NAME --project transpirafund-webapp. See CLAUDE.md Cloud Functions ownership. && exit 1",
    "logs": "firebase functions:log",
    "test": "jest"
  },
  "engines": {
    "node": "22"
  },
  "main": "src/index.js",
  "dependencies": {
    "@anthropic-ai/sdk": "^0.90.0",
    "firebase-admin": "^13.10.0",
    "firebase-functions": "^7.2.5",
    "nodemailer": "^9.0.3",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "firebase-functions-test": "^3.4.1",
    "jest": "^29.7.0"
  },
  "private": true
}
